module Prelude
  ( module X
  ) where

import           Diagrams.Prelude as X
import           Protolude        as X hiding (First, Last, Strict, conjugate,
                                        from, getFirst, getLast, local, rotate,
                                        shift, to, trace, uncons, unsnoc, zero)
import           Unsafe           as X
